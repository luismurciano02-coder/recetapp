// Configuración de producción. Cuando se compila con la configuración
// "production", Angular sustituye automáticamente environment.ts por este
// archivo (ver fileReplacements en angular.json).
export const environment = {
  production: true,
  apiUrl: 'https://tu-dominio.com/api'
};
